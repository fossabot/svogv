import { Component } from '@angular/core';

@Component({
	selector: 'data-set',
	templateUrl: './data-set.component.html',
	styleUrls: ['./data-set.component.scss']
})

export class DataSetComponent {

	// ToDo: Think about following features to add:
	// loading-indicator to indicate data loading
	// empty data (no children) handling
	// a flag to toggle responsive behaviour
	// a breakpoint property to allow responsive customization
	// reporting scroll status
	// fade out content
}
